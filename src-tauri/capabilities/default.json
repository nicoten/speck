{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: open a folder, open external links, check for and install app updates. No filesystem write access is granted; project files are read through the app\u2019s own read-only commands.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "opener:default",
    "dialog:allow-open",
    "updater:default",
    "process:allow-restart"
  ]
}